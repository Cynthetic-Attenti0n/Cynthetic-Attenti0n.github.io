// Configuration settings for the PDF Editor
export const config = {
    // Maximum number of pages to display in the editor at once
    maxPagesToShow: 100,
    
    // Default page scaling for initial PDF display
    defaultScale: 1.0,
    
    // Thumbnail size configuration
    thumbnails: {
        width: 150,
        quality: 0.5
    },
    
    // Rotation increment in degrees
    rotationIncrement: 90,
    
    // Enable debug mode for additional console logging
    debugMode: false
};

